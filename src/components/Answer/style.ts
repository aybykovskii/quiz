import { makeStyles } from "@material-ui/styles";

export const useStyle = makeStyles({
  answerItem: {
    width: "400px",
    height: "20px",
    border: "2px solid #fff",
    borderRadius: "4px",
    padding: "10px",
    margin: "0 auto",
    marginTop: "15px",
    cursor: "pointer",
    transition: "0.5s",
  },
});
